# 所有文件按原样存取，不做换行符转换。
#
# 为什么要这条：Windows 上 git 默认 core.autocrlf=true，检出时会把 LF 改成 CRLF。
# 于是 cloud\vs-core.ps1 和根目录那份就变得字节不同 —— 内容一模一样，
# 但哈希对不上。本机的漂移自检会因此天天误报「云端在跑旧逻辑」，
# 报几次之后你就不看它了，等到真的漂移那天也不会有人注意。
#
# 告警只有在从不撒谎的时候才有用。
* -text
